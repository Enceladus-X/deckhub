# DeckHub Frontend

Next.js static export frontend for DeckHub.

## Commands

```powershell
npm install
npm run dev
npm run lint
npm run build
npm run preview
```

`next.config.js` enables `output: "export"` so production builds are emitted to `out/` for S3 + CloudFront deployment.
